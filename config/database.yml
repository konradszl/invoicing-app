default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: invoicing_app_development

test:
  <<: *default
  database: invoicing_app_test

production:
  primary: &primary_production
    <<: *default
    database: invoicing_app_production
    username: konrad
    password: <%= ENV["INVOICING_APP_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: invoicing_app_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: invoicing_app_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: invoicing_app_production_cable
    migrations_paths: db/cable_migrate
